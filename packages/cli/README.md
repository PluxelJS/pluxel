# @pluxel/cli

Pluxel CLI（对外发布包之一）。

它是用户侧的入口（build/scaffold/hmr 等命令），自身只依赖 `@pluxel/hmr` 作为能力聚合点。

文档入口：

- `docs/ARCHITECTURE.md`
- `docs/PACKAGING.md`
- `docs/AGENT_RULES.md`

常用：

```sh
pluxel build
pluxel hmr
pluxel new
```

