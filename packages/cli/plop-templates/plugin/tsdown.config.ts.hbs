import { defineConfig } from 'tsdown'

export default defineConfig({
	entry: './src/{{pluginName}}.ts',
	dts: {
		build: true,
		sourcemap: true,
	},
	plugins: [
		// 需要宏/transform 的话，下游自行添加：
		// import Macros from 'unplugin-macros/rolldown'
		// Macros(),
	],
	format: ['esm'],
	env: {},
	copy: [
		// 'assets/**'
	],
	clean: true,
	minify: true,
	treeshake: true,
	// 供 pluxel-cli build 覆盖
	external: [],
})
