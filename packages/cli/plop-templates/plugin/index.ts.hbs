// {{kebabCase name}}.ts
import { BasePlugin, Plugin } from '@pluxel/hmr'

@Plugin({ name: '{{kebabCase name}}', type: 'hook' })
export class {{kebabCase name}} extends BasePlugin {
	init(): void {
		this.ctx.logger.info('{{kebabCase name}} initialized')
	}

	doSomething(): void {
		// this.ctx.logger.info(this.ctx.caller, 'call from')
		this.ctx.logger.info('{{kebabCase name}} doing something...')
	}
}
