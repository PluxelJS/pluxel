// Ink optionally loads react-devtools-core when `DEV=true`.
// For the published CLI bundle we don't want a hard dependency on it.
export default {
	initialize() {
		// no-op
	},
	connectToDevTools() {
		// no-op
	},
}
