import { BasePlugin, Plugin, v } from '@pluxel/runtime'

const {{className}}Config = v.object({
	enabled: v.optional(v.boolean(), true),
})

@Plugin({ name: '{{className}}' })
export class {{className}} extends BasePlugin {
	private readonly config = this.configs.use({{className}}Config)

	get enabled(): boolean {
		return this.config.enabled
	}

	override init(): void {
		this.ctx.logger.info('{{className}} ready')
	}
}

export default {{className}}
