export const greetingSchemaSDL = /* GraphQL */ `
	type Query {
		greeting(name: String): String!
	}
`

export class GreetingService {
	greet(name: string, message: string): string {
		const normalized = name.trim() || 'world'
		return `${message}, ${normalized}!`
	}
}
