/**
 * Web SDK (browser/React).
 *
 * Public entry: `@pluxel/hmr/web`.
 *
 * Implementation lives in the internal package `@pluxel/hmr-web` to keep browser concerns
 * isolated from the host runtime. `@pluxel/hmr/web` stays as the public bridge entry.
 */

export * from '@pluxel/hmr-web'
