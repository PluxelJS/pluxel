Runtime compilation services:

- `bundler`: runs Vite `build()` inside a Tinypool worker (runtime bundling + optional on-disk cache).
- `extension`: watches and compiles plugin UI extension entries into single-file browser modules.

