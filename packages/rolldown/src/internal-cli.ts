/** Official CLI cooperation only; watch resources belong to the command process. */
export { resolveBuildContext } from './cli/config'
export { pluginPackage } from './cli/plugin-build'
export { runWithTsdown } from './cli/tsdown-runner'
