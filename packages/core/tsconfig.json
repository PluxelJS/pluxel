{
	"extends": "../../tsconfig.src.json",
	"include": ["src", "tests"],
	"compilerOptions": {
		"outDir": "dist",
		// Only output d.ts files and not JavaScript files.
		"emitDeclarationOnly": true
	},
	"references": []
}
