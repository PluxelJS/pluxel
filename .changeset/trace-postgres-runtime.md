---
'@pluxel/rolldown': patch
---

Trace PostgreSQL `pg` as a static application residual dependency so frozen Node distributions use
Node's native CommonJS/ESM interop instead of emitting an invalid bundled `pg-pool` superclass.
