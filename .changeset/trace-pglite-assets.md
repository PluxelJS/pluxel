---
'@pluxel/rolldown': patch
---

Keep PGlite external and fully trace its package into frozen Node applications so runtime assets
such as `pglite.data` remain available next to the deployed package.
