---
'@pluxel/runtime': minor
---

Add plugin-scoped native Workbench tabs and parameterized routes, reduce placements to the supported plugin-tab and route model, and isolate each Remote View in an explicit host environment.
