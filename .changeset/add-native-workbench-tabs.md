---
'@pluxel/runtime': minor
---

Add plugin-scoped native Workbench tabs and parameterized routes, replace the generic slot registry with explicit `tab()` and `route()` placements, and isolate each Remote View in an explicit host environment.
