# 安全策略

## 报告安全问题

请不要通过公开 Issue、Discussion 或 Pull Request 披露尚未修复的安全漏洞。

优先使用仓库 **Security** 页面中的 **Report a vulnerability** 私下提交报告。如果该入口不可用，请通过 [PluxelJS 组织主页](https://github.com/PluxelJS) 提供的维护者联系方式建立私密沟通渠道，再发送漏洞细节。

报告中请尽量包含：

- 受影响的 package、版本或 commit；
- 可复现步骤或最小示例；
- 实际影响和可能的攻击路径；
- 已知的缓解方式；
- 是否有公开时间线要求。

维护者会确认收到报告、评估影响，并在修复可用后协调披露。请在协调完成前避免公开细节。

## 支持范围

安全修复以 npm registry 中仍受支持的版本和 `main` 分支当前代码为准。`0.x` 阶段的具体支持版本会随发布说明公布；已停止支持的版本可能只提供升级建议。

一般错误、功能建议和不包含敏感细节的加固建议可以通过 GitHub Issues 提交。
