# Agent tool policy

Toolsets and Agent assignments are managed by standard Plugin configuration. The content below is a live, read-only projection of that policy and the shared command catalog.

## Current projection

::slot[status]

Missing command names remain in their Toolsets and assignments so they become available automatically when an owning Plugin publishes them. Commands outside every Toolset are listed as ungrouped and cannot enter an Agent catalog by accident.
