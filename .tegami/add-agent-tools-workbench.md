---
packages:
  '@pluxel/agent-tools':
    type: minor
---

## Add the Agent tools Workbench projection

Expose the Agent Tools policy/catalog snapshot as host-rendered Workbench Content. The live, read-only
projection groups published commands, Toolsets, missing memberships, Agent assignments, and ungrouped
commands without a Plugin-owned renderer or a second policy store.
